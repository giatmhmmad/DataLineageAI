# PROJECT_MEMORY.md

Dokumen ini adalah catatan persisten hasil repository discovery dan audit teknis project **Data Lineage EDA**. Tujuannya agar kontributor (manusia maupun AI assistant) di masa depan tidak perlu menemukan ulang temuan yang sama dari nol. **Update bagian "Riwayat Perubahan" setiap kali task dari `IMPLEMENTATION_PLAN.md` selesai dikerjakan.**

- Snapshot dibuat: 2026-08-26
- Berdasarkan: repository discovery penuh + audit 10 dimensi (arsitektur, maintainability, security, performance, code quality, testing, error handling, logging, configuration management, AI/LLM reliability)

---

## Ringkasan Project

Aplikasi Django untuk manajemen data lineage ETL (Bank Mandiri, Tim Data Management ODP-IT), dengan AI Chatbot berbasis Groq API dan integrasi n8n untuk otomasi deteksi upload job. Git repo berada di `data-lineage/` (bukan di root folder `V2`), berisi 81 file ter-track, ~20 commit, masih tahap pengembangan aktif.

## Fakta Arsitektur yang Sudah Terverifikasi

- Django monolith, satu app (`main`), tidak ada pemisahan app per domain.
- Dual-database: `default` (dikelola penuh via Django ORM/migrations) dan `bot_eda` (3 model `managed=False`, dimiliki n8n, diakses via `BotEdaRouter`).
- n8n terhubung dua arah: webhook masuk (`/api/n8n/lineage/`) dan akses langsung ke DB `bot_eda` (Postgres node + `postgresTrigger`). Workflow n8n memakai `@n8n/n8n-nodes-langchain.agent` + `lmChatGroq` untuk deteksi schema/kategori tabel.
- Chatbot: intent-detection rule-based (keyword Bahasa Indonesia) → query data manual → HTML tabel dibangun di Python → Groq hanya untuk narasi.
- Deployment: Railway (nixpacks, Gunicorn 3 workers) + Neon Postgres. Riwayat commit menunjukkan sempat dicoba di Render dan Koyeb sebelum settle di Railway.
- Tidak ada CI/CD pipeline (tidak ditemukan `.github/workflows` atau setara).
- `.env` dan `*.dump`/`*.sql` sudah benar di-`.gitignore`; **`settings.py` dengan kredensial hardcoded tetap ter-track**.

## Inventori Technical Debt (dari Audit)

### Critical
| # | Temuan | File | Kenapa Penting |
|---|---|---|---|
| C1 | Stored/reflected XSS — `build_html_table`/`build_detail_table` tidak escape nilai DB sebelum di-render via `innerHTML` | `main/chatbot_views.py`, `main/templates/chatbot.html:611` | Nilai `table_name`/`log_message` bisa berasal dari CSV upload atau webhook n8n eksternal tanpa validasi |
| C2 | Tidak ada autentikasi/otorisasi di seluruh aplikasi | `main/urls.py`, semua view | Semua operasi (termasuk delete) bisa diakses tanpa login |
| C3 | `SECRET_KEY` dan password DB hardcoded, ter-commit ke git | `data_lineage/settings.py:37,96,104` | Kredensial permanen di git history |

### High
| # | Temuan | File | Kenapa Penting |
|---|---|---|---|
| H1 | Webhook `receive_n8n_lineage` tanpa autentikasi/signature | `views.py:796` | Endpoint publik bisa membuat data arbitrer, juga vektor untuk C1 |
| H2 | Chatbot me-load seluruh dataset ke memori Python per-request + N+1 loop untuk `sessions_with_done` | `chatbot_views.py:302-360` | Tidak scalable seiring pertumbuhan data (README: 2969 relasi saat audit) |
| H3 | Nol test coverage | `main/tests.py` (boilerplate kosong), `requirements.txt` (tidak ada pytest) | Perubahan pada file besar (`views.py`, `chatbot_views.py`) tidak terverifikasi otomatis |

### Medium
| # | Temuan | File |
|---|---|---|
| M1 | `@csrf_exempt` dipakai luas termasuk endpoint internal (`chatbot_ask`, `api_add_relationship`, `api_delete_relationship`) | `views.py`, `chatbot_views.py` |
| M2 | Exception mentah (`str(e)`, `type(e).__name__`) dikembalikan ke client | `views.py`, `chatbot_views.py:1685` |
| M3 | `DEBUG` default fallback tetap `'True'` jika env var lupa di-set | `settings.py:221` |
| M4 | Intent-detection chatbot berbasis puluhan keyword yang saling overlap, rapuh terhadap perubahan | `chatbot_views.py` (`casual_kw`, `context_reset_kw`, `confused_kw`, dll) |
| M5 | Duplikasi pola pemanggilan Groq client (`get_groq_client()` vs `Groq(api_key=...)` langsung) | `chatbot_views.py` (~8+ lokasi) |
| M6 | `conversation_history` dari client dipercaya penuh tanpa validasi untuk menentukan konteks aktif | `chatbot_views.py:427-447` |
| M7 | Tidak ada rate limiting pada endpoint chatbot (memanggil Groq API berbayar) | `main/urls.py` (`chatbot_ask`) |
| M8 | Tidak ada retry/timeout pada panggilan Groq API | `chatbot_views.py` (semua `client.chat.completions.create`) |
| M9 | Prompt injection ringan — data user (nama job/tabel) masuk mentah ke prompt LLM | `chatbot_views.py` (context builder) |
| M10 | Cross-database write (`default` + `bot_eda`) dalam satu `transaction.atomic()` tidak menjamin konsistensi lintas-DB | `views.py` (`upload_job`) |

### Low
| # | Temuan | File |
|---|---|---|
| L1 | `Web/utils/seeding.py` mereferensikan model yang sudah dihapus (`ParentChildRelationship`, `ColumnUsedInRelationship`) | `Web/utils/seeding.py` |
| L2 | File migrasi lama di `main/migrations/backup/` — tidak dieksekusi tapi membingungkan | `main/migrations/backup/0011-0014...py` |
| L3 | Inkonsistensi dokumentasi model LLM (README vs `settings.py` default) | `README.md` vs `settings.py:181` |
| L4 | `trigger_n8n_webhook` — URL ngrok hardcoded, tampak seperti sisa kode test | `views.py:1639-1666` |
| L5 | Reset sequence manual via raw SQL pada setiap delete | `views.py` (`delete_table`, `delete_job`) |
| L6 | Logging hanya level `ERROR`, tidak ada audit trail aktivitas normal | `settings.py:184-205` |

## Area yang Bekerja Baik (Jangan Diubah Tanpa Alasan Kuat)

- `api_get_lineage` (BFS traversal iteratif, adjacency map in-memory, depth-limit) — desain performa yang sudah baik.
- Pemisahan tanggung jawab LLM (narasi) vs data (Python) di chatbot — mengurangi risiko halusinasi angka.
- `BotEdaRouter` — implementasi router database yang bersih dan idiomatik.
- Konfigurasi production (Railway/Neon) sudah terpisah rapi dari fallback lokal via pengecekan env var.
- `.gitignore` sudah benar mengecualikan `.env` dan file dump database.

## Keputusan / Area yang Sengaja Tidak Diubah Dulu

- **Refactor `main` app menjadi multiple Django apps** — nilai jangka panjang jelas, tapi ditunda karena project masih iterasi cepat; risiko merge conflict lebih besar dari manfaat saat ini.
- **Caching/read-replica untuk performa** — dataset masih relatif kecil (2969 relasi saat audit); optimasi ini prematur tanpa bukti profiling nyata.
- **Mengganti intent-detection rule-based dengan pure-LLM function-calling** — trade-off reliability vs fleksibilitas, butuh keputusan produk eksplisit, bukan keputusan teknis sepihak.
- **Mengubah `bot_eda` unmanaged models jadi `managed=True`** — akan memutus kontrak dengan n8n yang saat ini memiliki skema tersebut; butuh koordinasi lintas sistem.

## Pertanyaan Terbuka (Belum Terjawab Saat Audit)

- Isi detail node-by-node 2 workflow n8n belum dibaca penuh (hanya daftar tipe node yang teridentifikasi).
- Bagaimana persisnya autentikasi FTP untuk pengambilan file job `.zip` oleh n8n — belum ditemukan di kode Django.
- Isi `django_errors.log` belum dibaca — berpotensi berisi data sensitif produksi.
- Apakah `migrasi_db/*.dump` berisi data produksi asli (sensitif) atau data sample.
- Apakah ada rencana project lain di level folder `V2` di luar `data-lineage/` (repo git hanya ada di dalam `data-lineage/`).

## Riwayat Perubahan

- **2026-08-26** — Dokumen dibuat berdasarkan repository discovery dan audit awal. Belum ada task dari `IMPLEMENTATION_PLAN.md` yang dieksekusi.
- **2026-08-26** — Ditambahkan **Active Context & Session Memory** ke chatbot (`Web/main/chatbot_views.py`, `Web/main/templates/chatbot.html`). Backend kini mengembalikan `active_context` (last_job_name, last_table_name, last_relationship, last_upload_log, last_list) di setiap response sukses; frontend menyimpan dan mengirimkannya balik pada request berikutnya, sehingga follow-up question ("target table nya apa?", "developer nya siapa?", "status upload nya bagaimana?", "yang pertama...") bisa di-resolve tanpa user mengulang nama job. Mekanisme lama (`extract_active_context`/`resolve_question`, text-scan atas `conversation_history`) **tetap ada** sebagai fallback untuk client lama. Diverifikasi end-to-end lewat dev server + data real di local Postgres (job `RDBMS_CDP_NEW_RMTOOLS_DLY_H1`) — 7 skenario wajib + 2 skenario bonus PASS. Ini bukan bagian dari task terjadwal di `IMPLEMENTATION_PLAN.md` — merupakan permintaan fitur terpisah dari user.
  - **Known limitation baru yang ditemukan**: tidak ada intent handler "detail tabel" di chatbot ini — resolusi ke tabel (mis. "yang pertama" pada list target table) hanya dilayani lewat jalur LLM generik (STEP 6), bukan lewat data terstruktur seperti job. Juga ada mekanisme text-scan job-detection ketiga yang khusus di cabang `job_logs` (`chatbot_views.py`, sekitar deteksi ulang `mentioned_job` dari `conversation_history`) yang belum di-guard terhadap `mentioned_table` — celah sangat sempit (perlu kombinasi keyword log + referensi ordinal ke tabel dalam satu pertanyaan), tidak disentuh karena di luar scope task ini.
- **2026-08-26** — Fix regresi "sticky context" yang ditemukan user lewat testing manual frontend setelah task Active Context di atas. Root cause: 3 titik kode (`elif mentioned_job: intent='job_detail'` di elif-chain STEP 4, handler gabungan `list_data`/`job_detail`, handler `impact_analysis`) memakai `mentioned_job` sebagai sinyal prioritas mutlak tanpa mengecek apakah intent baru sudah self-contained (list semua job, greeting/casual, impact analysis agregat). Fix: (1) reorder elif-chain — greeting/casual/confused dicek sebelum fallback `mentioned_job`→`job_detail`; (2) guard `if mentioned_job and intent == 'job_detail':` di handler list; (3) guard `if mentioned_job and not is_impact_aggregate_query:` (keyword agregat: "yang gagal"/"semua job"/dst) di handler impact_analysis. Sekaligus perbaikan Bug B: `resolve_ordinal_index` diperluas dengan regex untuk ordinal angka eksplisit ("nomor 6", "ke-6"), sebelumnya hanya mengenali kata ("yang pertama"). Diverifikasi lewat replay 1 sesi percakapan berantai 7 langkah + 1 skenario bonus — semua PASS (lihat catatan cosmetic di bawah).
  - **Known cosmetic residual (belum diperbaiki, tidak blocking)**: saat intent='list_data' dengan active job masih tersimpan di context, narasi pembuka dari LLM kadang masih menyebut job aktif sebagai kalimat pertama sebelum menampilkan list 21 job (data/tabel yang ditampilkan sudah benar dan lengkap, ini murni gaya bahasa LLM) — disebabkan `active_job_info` diinjeksikan ke `job_list_context` (`chatbot_views.py` sekitar baris 1847) tanpa syarat. Tidak memengaruhi kebenaran data, hanya estetika narasi. **[Diperbaiki di entri berikutnya - Fase testing manual round 2]**
- **2026-08-26** — Fix 4 bug dari testing manual round kedua (ditemukan setelah Fase "Fix regresi sticky context" di atas):
  - **Bug C** ("job lain" tidak konsisten/salah): root cause - tidak ada keyword-list yang menangkap "job lain"/"lainnya"/"yang lain", jatuh ke jebakan `casual_kw` (kata `'ya'` adalah substring dari "yang"/"saya" - cacat lama, bukan regresi baru). Fix: tambah frasa job-switch ke `list_kw` sehingga selalu reuse `list_data` (tampilkan list 21 job) - deterministik, tidak pernah lagi mengklaim "hanya ada 1 job".
  - **Bug D** (ordinal "job nomor N" rusak setelah tabel ringkasan impact analysis): root cause - `build_last_list` untuk impact_analysis/job_logs/job_status/relationship_info menimpa `last_list` valid (21 job) dengan subset parsial (4 job bermasalah, dst), lalu index di luar jangkauan subset itu diam-diam gagal ke fallback "data belum tersedia". **Keputusan: Opsi A** (last_list HANYA di-update oleh list_data/source_tables/target_tables, intent lain preserve list lama) - `build_active_context_payload` sekarang terima `active_context_in` dan fallback preserve `last_list` kalau tidak ada list baru dieksplisitkan. Ditambah: index ordinal eksplisit di luar jangkauan sekarang dijawab jujur ("List terakhir cuma berisi N item...") bukan fallback generik menyesatkan.
  - **Bug E** (guard agregat Fase sebelumnya cuma di `impact_analysis`): logic `is_impact_aggregate_query` di-extract jadi helper reusable `is_aggregate_all_jobs_query()`, dipasang juga di `job_logs` (terbukti gagal), `developer_info`, `source_tables`, `target_tables`, dan **`job_status`** (temuan tambahan dari audit, pola sama persis meski tidak disebut eksplisit oleh user).
  - **Bug F** (narasi LLM rusak/typo/tidak nyambung saat list ditampilkan dengan job aktif tersimpan): root cause - `active_job_info` disuntik tanpa syarat ke semua narasi termasuk yang agregat, LLM dapat instruksi kontradiktif. Fix: hapus `active_job_info` dari narasi list_data (all-jobs), developer_info (all-dev), job_status (all-status), impact_analysis (agregat), relationship_info (selalu agregat). Ditambah 1 guard prompt ("jangan mulai dengan basa-basi 'terima kasih atas informasinya'") di 5 titik yang sama setelah residual quirk itu masih muncul sesekali di replay regresi.
  - Diverifikasi lewat replay real (data + Groq API asli): semua skenario wajib PASS.
  - **Known residual (di luar scope, dilaporkan transparan)**: (1) `build_last_list` masih membatasi `max_items=20`, sedangkan total job saat ini 21 - job urutan ke-21 tidak bisa diakses via "nomor 21" (pre-existing dari Fase 1, belum pernah dilaporkan sebagai bug, cukup jarang berdampak). (2) Typo transkripsi nama job oleh LLM (mis. "CDM_" vs "CDP_") masih bisa terjadi sesekali di narasi PER-JOB yang legitimate (job_detail, dst) - ini di luar scope Bug F (yang spesifik soal narasi list/agregat) karena merupakan reliabilitas transkripsi LLM yang inheren, bukan akibat active_job_info di context agregat. (3) Mekanisme text-scan job-detection redundan di dalam cabang `job_logs` (baris ~1256-1272) belum di-guard terhadap kasus agregat/table - celah sangat sempit, sudah dicatat sejak Fase 1B, belum disentuh.
- **2026-08-28** — Fix 3 bug dari testing manual round berikutnya (setelah Fase 1H), dengan diagnostic WAJIB (replay via Django test `Client` langsung ke `chatbot_ask`, tanpa memanggil Groq, untuk membaca `intent`/`mentioned_job`/`active_context` aktual) sebelum fix untuk Bug S dan Bug U:
  - **Bug S** ("saya mau lihat job X" dijawab generik, bukan job_detail): root cause dibuktikan lewat trace nyata - kata **'ya'** di `casual_kw` cocok sebagai SUBSTRING dari "**sa`ya`**" (dan "n`ya`"), jadi `any(k in question_lower for k in casual_kw)` bernilai True untuk kalimat apa pun yang mengandung kata "saya"/"nya" dengan ≤5 kata total, memaksa `intent='casual'` SEBELUM elif-chain sempat sampai ke `elif mentioned_job: intent='job_detail'` di baris paling akhir chain (mentioned_job sendiri sudah benar terdeteksi = nama job valid, tapi terlanjur kalah prioritas). Trace aktual: `explicit_job_in_question= DPK_H0_KLN mentioned_job= DPK_H0_KLN intent= casual` untuk pertanyaan "saya mau lihat job DPK_H0_KLN". "detailkan job saya mau lihat job DPK_H0_KLN" kebetulan lolos bukan karena kata "detail" dikenali - kalimat itu jadi 7 kata, melewati guard `len(question.split()) <= 5` milik casual_kw, sehingga fallback `elif mentioned_job` sempat kebagian giliran. Fix (general, bukan tambal per-frasa): tangkap `explicit_job_in_question` di STEP 3 (job valid yang match EKSPLISIT di teks pertanyaan SAAT INI, bukan carry-over context/riwayat) lalu prioritaskan sebagai `job_detail` SEBELUM greeting/casual/confused di elif-chain STEP 4 - berlaku untuk kalimat apa pun ("saya ingin lihat", "saya mau lihat", dst) yang menyebut nama job valid, bukan cuma frasa yang dilaporkan. Ditemukan+diperbaiki juga edge-case turunan: `is_reset` (context-reset detector) sebelumnya tetap mengosongkan `mentioned_job` untuk kalimat seperti "oke lihat job X" (kata "oke" ada di `context_reset_kw`), membuat intent dipaksa 'job_detail' oleh fix di atas TAPI `mentioned_job` kosong - handler gabungan `list_data`/`job_detail` lalu jatuh ke cabang list_data (guard butuh `mentioned_job` terisi). Ditutup dengan menambah `not explicit_job_in_question` ke kondisi `is_reset`.
  - **Bug T** ("tampilkan ulang dari halaman pertama" pada list halaman 2 dijawab pesan out-of-range): root cause - kata "pertama" ada di `ORDINAL_KEYWORDS` (idx=0), jadi `resolve_ordinal_index` menangkapnya sebagai referensi ke ITEM urutan ke-1 sebelum sempat dicek apakah ini permintaan reset pagination. Item ke-1 ada di halaman SEBELUM halaman yang sedang dilacak (offset 20) → `local_idx = 0 - 20 = -20 < 0` → jatuh ke pesan out-of-range "ada di halaman sebelumnya" (pesan yang sama persis dengan kasus genuinely out-of-range, sesuai laporan). Fix: `PAGE_RESET_KW` (`'halaman pertama'`, `'halaman pertaman'` [typo yang ditemukan saat testing, didaftarkan literal], `'halaman 1'`, `'halaman awal'`, `'dari awal'`) dicek SEBELUM `resolve_ordinal_index`/`resolve_from_active_context` dipanggil - kalau ada list job berpaginasi aktif (`last_list_job.page_info`) dan pertanyaan match salah satu, langsung `render_job_list_page(0, ...)` (reset ke halaman 1 dari list YANG SAMA), sejalur dengan mekanisme page-advance "lanjut ke halaman berikutnya" yang sudah ada.
  - **Bug U** (setelah "job nomor 21" berhasil lalu "job nomor 1" out-of-range, "tampilkan ulang" menampilkan job LAMA `DPK_H0_KLN` alih-alih job nomor 21): root cause dibuktikan lewat trace nyata `active_context.last_job_name` di tiap turn - handler `out_of_range_message`/`clarification_message` (dipakai saat resolusi ordinal gagal) memanggil `build_active_context_payload(None, None, ...)`, yang menimpa `last_job_name` jadi `None` walau job aktif sebelumnya (`SPARK_DMT_WHL_DM_COFYOL_DAILY_DLY_H1`, dari "job nomor 21" yang baru saja sukses) masih valid - referensi ordinal yang gagal BUKAN pergantian topik, tapi kode memperlakukannya seolah begitu. Efek berantai: turn berikutnya ("tampilkan ulang", tidak menyebut job apa pun) gagal resolve lewat `last_job_name` (sudah kosong), jatuh ke fallback LAMA (`extract_active_context`/`resolve_question`) yang scan ULANG SELURUH `conversation_history` mentah dari teks - menemukan mention job valid PALING LAMA yang masih ada di history (`DPK_H0_KLN`, dari turn Bug S jauh sebelumnya), karena turn "job nomor 21"/"job nomor 1"/"tampilkan ulang" sendiri tidak pernah menyebut nama job secara harfiah (semua lewat resolusi ordinal). Dikonfirmasi juga: kegagalan Bug S tidak ikut mengosongkan active_job (jalur casual lama tetap memanggil `build_active_context_payload(mentioned_job, ...)` dengan mentioned_job yang benar) - satu-satunya titik yang mengosongkan adalah 2 early-return ordinal-gagal di atas. Fix: kedua early-return sekarang preserve `active_context_in.get('last_job_name')`/`last_table_name'` alih-alih memaksa `None, None`.
  - Semua 3 fix diverifikasi via replay langsung ke `chatbot_ask` (Django test `Client`, data real, tanpa Groq untuk bagian intent/context; dengan Groq asli untuk cek narasi akhir) - skenario wajib dari laporan + variasi tambahan ("saya ingin lihat job X", "tampilkan job X", typo "halaman pertaman") + regresi Fase 1B/1D/1F/1G/1H/Bug C/Bug Q (follow-up implisit "nya", ordinal angka, "ke 2" vs "ke 21", "job lain", greeting/casual/confused dengan active job, impact analysis agregat) - semua PASS.
  - **Known residual (di luar scope, ditemukan saat regression, TIDAK diperbaiki)**: pola sama seperti Bug S ('ya' substring trap) juga terjadi untuk referensi ORDINAL ke TABEL, mis. "yang pertama itu apa" (mengandung "**ya**ng") ikut jatuh ke `casual_kw` walau ada `last_list_table` aktif - beda dari Bug S karena resolusinya bukan nama job eksplisit (tidak ada intent handler "detail tabel" sama sekali, sudah dicatat sebagai limitation sejak entri Active Context pertama). Tidak disentuh - di luar 3 bug yang diminta, dan memperbaikinya butuh keputusan desain terpisah (menambah intent handler tabel) bukan sekadar guard elif-chain. **[Diperbaiki sebagian di entri berikutnya - Bug S lanjutan, lihat di bawah: kasus "yang pertama itu apa" sekarang benar SELAMA ada last_list_table aktif dan resolusinya berhasil, via `ordinal_resolved_this_turn`]**
- **2026-08-28 (lanjutan)** — Bug S ternyata belum tuntas di fix sebelumnya (fix sebelumnya cuma menutup kasus "nama job eksplisit disebut", bukan akar masalah `casual_kw` itu sendiri). Audit ulang dengan diagnostic wajib (trace nilai variabel aktual via replay langsung ke `chatbot_ask`, tanpa Groq untuk bagian intent):
  - **Bukti 1** ("saya mau lihat job" tanpa nama job -> "Siap! Ada lagi?", diulang identik -> kadang benar kadang salah): trace membuktikan root cause SEBENARNYA ada di `casual_kw` sendiri - kata `'ya'` dicocokkan dengan **substring** (`k in question_lower`), bukan word-boundary, jadi match di dalam "sa**ya**"/"n**ya**"/"han**ya**" apa pun ada/tidaknya nama job. Fix sebelumnya (`explicit_job_in_question`) hanya menutup kasus nama job eksplisit disebut - kalimat TANPA nama job sama sekali ("saya mau lihat job") tetap kena. Untuk "input identik, output beda": trace membuktikan KEDUA request menghasilkan `intent='casual'` yang SAMA persis (tidak ada state/variable yang berbeda) - bedanya murni pada narasi Groq: handler `casual` menyuntik pesan user mentah ke prompt lalu menyertakan CONTOH kalimat kanonik ("Siap! Ada lagi yang ingin ditanyakan?") untuk kasus "oke/baik/ya/sip" - karena intent yang mendasarinya SALAH, LLM harus menebak pola mana yang cocok, dan tebakan itu tidak stabil (Groq temperature=0.1, bukan 0, tetap punya variansi sampling). Fix (general, sesuai prinsip yang diminta - BUKAN tambal per-frasa): `matches_any_keyword_wordwise()` - regex `\b...\b` per keyword, dipasang HANYA pada pengecekan `casual_kw` (scope yang diminta). Diverifikasi: "saya mau lihat job"/"nya gimana"/"hanya itu saja" semua lepas dari `casual`, jatuh ke `general` (jalur LLM generik yang benar, tanya balik nama job) - dan replay 2x identik sekarang KONSISTEN (`general` kedua kali, hanya redaksi Groq yang bervariasi, bukan intent/kebenarannya).
  - **Bukti 2** ("iya job 19" setelah disambiguasi Bug K -> "Siap! Ada lagi?" alih-alih resolve ke job nomor 19): trace membuktikan word-boundary SAJA TIDAK CUKUP - `'iya'` bukan jebakan substring, dia literal terdaftar sebagai keyword UTUH di `casual_kw` (untuk kasus afirmasi murni seperti "iya benar", ini memang perilaku benar, dikonfirmasi tetap `casual` setelah fix). Root cause sebenarnya: `resolve_from_active_context` SUDAH BENAR meresolve "job 19" ke job yang tepat lewat pola ordinal eksplisit (`ORDINAL_NUMBER_ANCHORED_PATTERN`, anchor kata "job"+angka) SEBELUM STEP 4 intent detection - tapi keberhasilan resolusi itu tidak dianggap "sinyal eksplisit" di elif-chain, jadi `casual_kw` ('iya') tetap menang duluan. Sempat dipresentasikan trade-off-nya ke user sebelum implementasi (sesuai permintaan eksplisit) - opsi yang dipilih: generalisasi prinsip yang SAMA dengan fix `explicit_job_in_question` (Bug S versi pertama), diperluas ke "referensi ordinal eksplisit yang berhasil di-resolve turn ini". Implementasi: `resolve_from_active_context` sekarang menandai `result['ordinal_resolved']=True` HANYA di 3 titik keberhasilan resolusi ordinal (bukan di fallback implisit "nya"/last_job_name di bagian 2 fungsi yang sama - pemisahan ini penting, supaya tidak membangkitkan lagi regresi Active Context lama yakni stale carry-over context membajak greeting/casual). Variabel `ordinal_resolved_this_turn` di `chatbot_ask` dipakai di slot prioritas yang sama dengan `explicit_job_in_question`: `intent='job_detail' if mentioned_job else 'general'` (kalau resolusinya ke tabel, tidak dipaksa job_detail - tidak ada intent handler tabel, dibiarkan jatuh ke 'general' yang sudah otomatis pakai `active_table_info`).
  - Diverifikasi: "iya job 19" -> `job_detail` dengan job yang benar. "iya" saja (tanpa nomor) dan "oke makasih" tetap `casual` (tidak overreach). Verifikasi tambahan dari laporan sebelumnya (pending Fase 1I): ordinal KATA "yang pertama"/"yang terakhir" tanpa kata "halaman" tetap resolve normal (tidak ke-treat sebagai page-reset oleh `PAGE_RESET_KW`); nama job eksplisit + keyword intent spesifik (source/target/developer/status) tetap jatuh ke intent spesifiknya, bukan ke-shortcut job_detail. Regresi Fase 1B/1D/1F/1G/1H/1I (ordinal angka, "ke 2" vs "ke 21", "job lain", greeting/casual dengan active job, impact analysis agregat, page-reset "halaman pertama") - semua PASS.
  - **Known residual (di luar scope, TIDAK diperbaiki)**: "coba job itu ya"/"banyak job ya" (kata "ya" berdiri sendiri sebagai partikel afirmasi di akhir kalimat, bukan substring) masih match `casual_kw` secara word-boundary - ini sengaja tidak disentuh karena "ya" sebagai kata mandiri adalah pemakaian valid dalam Bahasa Indonesia (partikel penegas/tag-question), bukan bug seperti substring trap, dan tidak ada di antara 2 bukti yang dilaporkan.
