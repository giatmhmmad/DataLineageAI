[phases.setup]
nixPkgs = ["python312", "postgresql"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[phases.build]
cmds = ["./build.sh"]

[start]
cmd = "gunicorn data_lineage.wsgi:application --bind 0.0.0.0:$PORT"